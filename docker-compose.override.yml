# Local development overrides — exposes ports directly for testing without DNS
services:
  evolution-api:
    ports:
      - "8080:8080"

  streamlit:
    ports:
      - "8501:8501"

  postgres:
    ports:
      - "5432:5432"
