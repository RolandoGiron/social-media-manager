services:
  postgres:
    ports:
      - "5432:5432"

  n8n:
    ports:
      - "5678:5678"
    environment:
      N8N_SECURE_COOKIE: "false"

  caddy:
    environment:
      ACME_EMAIL: dev@localhost
    volumes:
      - ./caddy/Caddyfile.dev:/etc/caddy/Caddyfile
