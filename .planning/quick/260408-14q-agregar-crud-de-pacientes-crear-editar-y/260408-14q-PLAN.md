---
phase: quick
plan: 260408-14q
type: execute
wave: 1
depends_on: []
files_modified:
  - admin-ui/src/components/database.py
  - admin-ui/src/pages/3_Pacientes.py
autonomous: true
requirements: [CRUD-create, CRUD-edit, CRUD-delete]
must_haves:
  truths:
    - "User can create a new patient via form below the table"
    - "User can edit an existing patient by selecting a row"
    - "User can delete individual patient via trash icon in expanded row"
    - "User can bulk-delete selected patients via action bar button with confirmation"
    - "Phone validation uses normalize_sv_phone on create and edit"
    - "Deleting a patient cascades to patient_tags (DB handles via ON DELETE CASCADE)"
  artifacts:
    - path: "admin-ui/src/components/database.py"
      provides: "insert_patient, update_patient, delete_patients, fetch_patient_by_id functions"
    - path: "admin-ui/src/pages/3_Pacientes.py"
      provides: "Create/edit form expander, bulk delete button, individual delete in row expander"
  key_links:
    - from: "admin-ui/src/pages/3_Pacientes.py"
      to: "admin-ui/src/components/database.py"
      via: "import insert_patient, update_patient, delete_patients, fetch_patient_by_id"
    - from: "admin-ui/src/pages/3_Pacientes.py"
      to: "admin-ui/src/components/patients.py"
      via: "import normalize_sv_phone for phone validation in form"
---

<objective>
Add full CRUD operations to the Pacientes page: create patient manually, edit existing patient data, and delete patients (individually and in bulk).

Purpose: Currently patients can only be imported via CSV. The clinic needs to add/edit/delete patients directly from the UI.
Output: Updated database.py with CRUD functions, updated 3_Pacientes.py with create/edit form and delete controls.
</objective>

<execution_context>
@/home/rolando/Desarrollo/social-media-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/rolando/Desarrollo/social-media-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@admin-ui/src/components/database.py
@admin-ui/src/components/patients.py
@admin-ui/src/pages/3_Pacientes.py
@postgres/init/001_schema.sql

<interfaces>
<!-- Existing DB patterns to follow -->
From admin-ui/src/components/database.py:
```python
def get_connection():
    return psycopg2.connect(os.environ.get("DATABASE_URL"))

# Pattern: conn = get_connection(), try/finally conn.close(), use RealDictCursor
# Pattern: conn.commit() after writes
# Pattern: execute_values for batch ops
```

From admin-ui/src/components/patients.py:
```python
def normalize_sv_phone(raw: str) -> tuple[str, str | None]:
    # Returns (normalized, error_message). error_message is None if valid.
```

From postgres/init/001_schema.sql:
```sql
CREATE TABLE patients (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    first_name TEXT NOT NULL,
    last_name TEXT NOT NULL,
    phone TEXT NOT NULL,
    phone_normalized TEXT NOT NULL,
    email TEXT,
    notes TEXT,
    consent_status BOOLEAN NOT NULL DEFAULT false,
    source TEXT DEFAULT 'csv_import',
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    CONSTRAINT uq_patients_phone_normalized UNIQUE (phone_normalized)
);
-- patient_tags has ON DELETE CASCADE on patient_id -- no manual cleanup needed
```

Current page state keys: pacientes_mode ("list"|"import"), current_page, import_preview, import_file_processed
Current bulk action bar: lines 303-334 of 3_Pacientes.py (tag assignment only)
Current table: st.dataframe with on_select="rerun", selection_mode="multi-row", key="patient_table"
fetch_patients returns: id, first_name, last_name, phone_normalized, source, notes, created_at
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CRUD database functions</name>
  <files>admin-ui/src/components/database.py</files>
  <action>
Add four new functions to database.py, following the existing pattern (get_connection, try/finally, RealDictCursor, conn.commit after writes):

1. `fetch_patient_by_id(patient_id: str) -> dict | None` -- SELECT all columns (first_name, last_name, phone, phone_normalized, email, notes, source) from patients WHERE id = %s. Return dict or None. Used to populate edit form.

2. `insert_patient(first_name: str, last_name: str, phone: str, phone_normalized: str, email: str = "", notes: str = "", source: str = "manual") -> dict` -- INSERT INTO patients (first_name, last_name, phone, phone_normalized, email, notes, source) VALUES (...) RETURNING id, first_name, last_name, phone_normalized. Note source default is "manual" (not "csv_import"). Let psycopg2 raise on duplicate phone_normalized (unique constraint) -- the caller handles the exception.

3. `update_patient(patient_id: str, first_name: str, last_name: str, phone: str, phone_normalized: str, email: str = "", notes: str = "") -> dict` -- UPDATE patients SET first_name=%s, last_name=%s, phone=%s, phone_normalized=%s, email=%s, notes=%s WHERE id=%s RETURNING id, first_name, last_name, phone_normalized. The updated_at trigger handles timestamp automatically.

4. `delete_patients(patient_ids: list[str]) -> int` -- DELETE FROM patients WHERE id = ANY(%s::uuid[]). Return cur.rowcount. patient_tags cascade is handled by the DB constraint -- no manual DELETE needed.

Add the new function names to the module's natural ordering (after existing insert_patients function).
  </action>
  <verify>
    <automated>cd /home/rolando/Desarrollo/social-media-manager && python -c "from admin-ui.src.components.database import insert_patient, update_patient, delete_patients, fetch_patient_by_id; print('imports OK')" 2>/dev/null || python -c "
import sys; sys.path.insert(0, 'admin-ui/src')
from components.database import insert_patient, update_patient, delete_patients, fetch_patient_by_id
print('All 4 functions imported successfully')
"</automated>
  </verify>
  <done>Four new functions exist in database.py: fetch_patient_by_id, insert_patient, update_patient, delete_patients. All follow the existing conn pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Add create/edit form and delete controls to Pacientes page</name>
  <files>admin-ui/src/pages/3_Pacientes.py</files>
  <action>
Modify the LIST MODE section of 3_Pacientes.py to add three UI features. Import the new DB functions and normalize_sv_phone at the top.

**A) Bulk delete button in the action bar (lines ~303-334):**
Inside the existing `if event and event.selection and event.selection.rows:` block, add a "Borrar seleccionados" button next to the existing "Asignar etiquetas" button. Use a 3-column layout: [tag multiselect (2), assign button (1), delete button (1)] or keep the existing 2-col for tags and add a new row below. Preferred: add `bulk_col_delete` column alongside existing bulk_col_tags and bulk_col_btn.

Implementation:
- Change `bulk_col_tags, bulk_col_btn = st.columns([3, 1])` to `bulk_col_tags, bulk_col_btn, bulk_col_delete = st.columns([3, 1, 1])`
- In bulk_col_delete: st.markdown("<br>", unsafe_allow_html=True) for vertical alignment, then st.button("Borrar seleccionados", icon=":material/delete:", type="secondary", use_container_width=True)
- When clicked, set session_state flag `confirm_bulk_delete = True` and `bulk_delete_ids = selected_ids`
- Show confirmation with st.warning(f"Estas a punto de borrar {len(ids)} pacientes. Esta accion no se puede deshacer.") and a "Confirmar borrado" button
- On confirm: call delete_patients(ids), show st.success(f"Se borraron {count} pacientes."), st.rerun()

**B) Individual delete via row expanders:**
After the dataframe and bulk action bar, but before pagination, iterate over `event.selection.rows` (if exactly 1 selected). For each selected patient row, show an st.expander with the patient's name showing patient details and a delete button (trash icon). This doubles as the entry point for editing (see C below).

Actually, simpler approach per the CONTEXT decisions: use expanders for EACH selected row. For each selected row index from event.selection.rows:
- Get the patient data from display_df.iloc[i]
- Use st.expander(f"Paciente: {row['Nombre']}", expanded=True) 
- Inside: show a 2-column layout with patient details and a delete button (individual)
- The delete button calls delete_patients([row_id]) with a confirmation step

**C) Create/Edit form as st.expander below the table:**

Add session state key: `editing_patient_id` (None = create mode, UUID = edit mode).

"Nuevo paciente" expander -- always visible below the table (before pagination):
- st.expander("Nuevo paciente", expanded=st.session_state.get("show_create_form", False))
- Form fields using st.text_input: nombre (required), apellido (required), telefono (required), email, notas, fuente (default "manual")
- On submit: validate nombre/apellido/telefono not empty, call normalize_sv_phone(telefono) -- if error show st.error, else call insert_patient(...) with normalized phone as both phone and phone_normalized
- On success: st.success, st.rerun()
- Handle duplicate phone (psycopg2.errors.UniqueViolation or generic Exception with "uq_patients_phone_normalized"): st.error("Ya existe un paciente con ese numero de telefono.")

"Editar paciente" expander -- shown when exactly 1 row is selected:
- When 1 row selected, fetch full patient data via fetch_patient_by_id(selected_id)
- Show st.expander(f"Editar: {patient['first_name']} {patient['last_name']}", expanded=True)
- Same form fields as create but pre-populated with existing values
- Phone field shows current phone value; if changed, re-validate with normalize_sv_phone
- Submit calls update_patient(...)
- Also show individual delete button ("Borrar paciente") with confirmation inside this expander
- On success: st.success, st.rerun()

**Update imports at top of file:**
Add to the database import: `fetch_patient_by_id, insert_patient, update_patient, delete_patients`
Add to the patients import: `normalize_sv_phone`

**Update fetch_patients to also return email:**
In database.py fetch_patients query, add `p.email` to the SELECT columns (currently missing). This is needed to populate the edit form.
  </action>
  <verify>
    <automated>cd /home/rolando/Desarrollo/social-media-manager && python -c "
import sys; sys.path.insert(0, 'admin-ui/src')
import ast
with open('admin-ui/src/pages/3_Pacientes.py') as f:
    source = f.read()
tree = ast.parse(source)
# Verify key strings exist in the source
checks = [
    'insert_patient' in source,
    'update_patient' in source,
    'delete_patients' in source,
    'normalize_sv_phone' in source,
    'Nuevo paciente' in source,
    'Borrar' in source,
    'fetch_patient_by_id' in source,
]
assert all(checks), f'Missing elements: {[i for i,c in enumerate(checks) if not c]}'
print('All CRUD UI elements present in source')
print(f'Source compiles: {tree is not None}')
"</automated>
  </verify>
  <done>
- "Nuevo paciente" expander with form (nombre, apellido, telefono, email, notas, fuente) creates patients with phone validation
- Selected row shows "Editar" expander with pre-populated form and individual delete button
- Bulk action bar includes "Borrar seleccionados" with confirmation dialog
- All operations show success/error feedback and rerun to refresh the table
  </done>
</task>

</tasks>

<verification>
1. Python syntax valid for both modified files (ast.parse)
2. All new DB functions importable
3. Page source contains all CRUD elements (create form, edit form, delete buttons, normalize_sv_phone usage)
4. No regression: existing import mode, tag management, pagination, search/filter untouched
</verification>

<success_criteria>
- A clinic admin can create a new patient manually with nombre+apellido+telefono (required) + email+notas+fuente (optional)
- A clinic admin can select a patient row and edit their data in an expander form
- A clinic admin can delete a single patient from the edit expander
- A clinic admin can select multiple patients and bulk-delete them with confirmation
- Phone numbers are validated with normalize_sv_phone on create and edit
- Duplicate phone numbers show a clear error message
- All existing functionality (import, search, tags, pagination) remains intact
</success_criteria>

<output>
After completion, create `.planning/quick/260408-14q-agregar-crud-de-pacientes-crear-editar-y/260408-14q-SUMMARY.md`
</output>
