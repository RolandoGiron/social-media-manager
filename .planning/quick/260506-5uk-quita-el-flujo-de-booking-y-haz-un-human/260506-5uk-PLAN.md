# Quick Task 260506-5uk: Remove Booking Flow + Human Handoff + Exact FAQ Answers

**Date:** 2026-05-06
**Status:** Ready for execution

## Goal

1. Quitar el flujo automático de booking (sub-booking-flow)
2. Cuando se detecta intent BOOKING o hay estado booking_flow activo → human handoff
3. Mejorar el sub-faq-answer para que retorne la respuesta exacta de la FAQ (no generada)
4. Arreglar la BD: agregar `promocion` al CHECK constraint; reasignar categorías `general` de FAQs sobre tratamientos a `servicios`

## Tasks

### Task 1: Remove booking flow, add human handoff for BOOKING intent

**File:** `n8n/workflows/whatsapp-chatbot.json`

Changes via Python script:
- Add new Set node "Set Booking Handoff Response" (position [3600, 200]) with message específico para booking + new_state=human_handoff
- Add new Code node "Set Active Booking Handoff" (position [2880, 400]) para cuando booking_flow ya está activo
- Connection: "Route by Intent" BOOKING (index 1) → "Set Booking Handoff Response" (was "Start Booking Flow")
- Connection: "Is Booking Flow Active?" True (index 0) → "Set Active Booking Handoff" (was "Continue Booking Flow")
- Connection: "Set Booking Handoff Response" → "Merge Intent Responses"
- Connection: "Set Active Booking Handoff" → "Merge All Response Paths"
- Update "Set Greeting Response" text: remove mention of "agendar una cita"

### Task 2: Update sub-faq-answer to return exact FAQ responses

**File:** `n8n/workflows/sub-faq-answer.json`

Change system prompt in "Generate FAQ Answer (OpenAI)": instruct OpenAI to return EXACTLY the respuesta from the best matching FAQ, not generate new text.

### Task 3: Fix knowledge_base categories in DB + migration

**File:** `postgres/init/003_knowledge_base.sql` (and new migration)

- Add `promocion` to the CHECK constraint
- Write migration SQL to update constraint and reclassify `general` FAQs about treatments to `servicios`
- Create `postgres/migrations/001_fix_knowledge_base_categories.sql`

Also update the n8n chatbot to sync: the Load Knowledge Base query should include `id` so the FAQ answer can reference exact rows.

## must_haves

- BOOKING intent → human_handoff state, specific booking message
- booking_flow state (legacy) → human_handoff  
- FAQ answer returns exact respuesta text from DB, not generated
- `promocion` category valid in CHECK constraint
- migration file exists for production deployment
