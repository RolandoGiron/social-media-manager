# Quick Task 260506-e4s: remove booking flow add human handoff for appointments update FAQ categories for OpenAI intent matching deploy n8n workflows

**Date:** 2026-05-06
**Directory:** .planning/quick/260506-e4s-remove-booking-flow-add-human-handoff-fo/

## Context

The booking flow removal and human handoff are already in the JSON files (commit 125b575). What remains:
1. Update FAQ categories in DB (3 rows have wrong categories)
2. Include `[categoria]` prefix in formatted FAQs so OpenAI can use it for selection
3. Update sub-faq-answer system prompt to use category context
4. Deploy whatsapp-chatbot and sub-faq-answer to n8n production

## Tasks

### Task 1 — Fix FAQ categories in database
**Files:** N/A (SQL via docker exec)
**Action:**
- `71f43488` (`¿Puedo cancelar o reagendar la cita?`) → `general` → `citas`
- `6c1b5b8d` (`¿Cuánto cuesta el tratamiento de rejuvenecimiento?`) → `precios` → `promocion`
- `37b6d8bb` (`¿El precio de $79.99 incluye todo?`) → `precios` → `promocion`
**Verify:** `SELECT id, categoria FROM knowledge_base WHERE id IN (...)` shows updated values

### Task 2 — Update Format FAQs for LLM node (whatsapp-chatbot.json)
**Files:** `n8n/workflows/whatsapp-chatbot.json`
**Action:** Change `format-faqs` node JS code to include `[${item.json.categoria}]` prefix:
```
`[${item.json.categoria}] Q: ${item.json.pregunta}\nA: ${item.json.respuesta}`
```
**Verify:** Node code in JSON contains `item.json.categoria`

### Task 3 — Update sub-faq-answer system prompt
**Files:** `n8n/workflows/sub-faq-answer.json`
**Action:** Update system prompt in `openai-faq-answer` node to mention category prefixes and instruct OpenAI to use them for narrowing down the relevant FAQ.
**Verify:** Node jsonBody contains category instruction

### Task 4 — Deploy to n8n production
**Files:** N/A (n8n MCP API calls)
**Action:** Use n8n API to push updated `whatsapp-chatbot` (ID: OIGLydhMfzLbjqGC) and `sub-faq-answer` (ID: BHvg7aS40fuVcnDB) workflows via PUT to n8n REST API
**Verify:** n8n API returns 200, workflow updatedAt timestamp changes

## must_haves
- FAQ categoria column updated in DB for 3 rows
- whatsapp-chatbot.json Format FAQs node includes [categoria] prefix
- sub-faq-answer.json system prompt mentions categories
- Both workflows deployed to n8n (updatedAt changes)
