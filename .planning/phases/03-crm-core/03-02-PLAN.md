---
phase: 03-crm-core
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - admin-ui/src/pages/3_Pacientes.py
autonomous: true
requirements:
  - CRM-01
  - CRM-02
  - CRM-03

must_haves:
  truths:
    - "Admin can upload CSV/Excel and see a preview with Nuevo/Duplicado/Error status badges before committing"
    - "Admin can confirm import and see success message with count of imported patients"
    - "Admin can search patients by name (3+ chars trigram ILIKE) or phone number"
    - "Admin can filter patients by one or more tags via multiselect"
    - "Admin can select patient rows and bulk-assign tags"
    - "Admin can create a new tag with name and color inline"
    - "Admin can delete unassigned tags; assigned tags show blocking message"
    - "Patient list is paginated at 25 rows per page"
  artifacts:
    - path: "admin-ui/src/pages/3_Pacientes.py"
      provides: "Complete patient management page: import, list, search, filter, tag CRUD, bulk assign"
      min_lines: 200
  key_links:
    - from: "admin-ui/src/pages/3_Pacientes.py"
      to: "admin-ui/src/components/database.py"
      via: "imports fetch_patients, insert_patients, fetch_existing_phones, fetch_tags_with_counts, create_tag, delete_tag, assign_tags_to_patients"
      pattern: "from components.database import"
    - from: "admin-ui/src/pages/3_Pacientes.py"
      to: "admin-ui/src/components/patients.py"
      via: "imports normalize_mx_phone, parse_import_file, build_preview"
      pattern: "from components.patients import"
    - from: "admin-ui/src/pages/3_Pacientes.py"
      to: "admin-ui/src/components/sidebar.py"
      via: "calls render_sidebar() at top"
      pattern: "render_sidebar\\(\\)"
---

<objective>
Build the Pacientes Streamlit page — the main CRM interface for patient import, listing, search, filtering, and tag management.

Purpose: Delivers CRM-01 (CSV import with normalization), CRM-02 (patient list with search/filter), and CRM-03 (tag creation, assignment, deletion). This is the core data management UI that Phase 5 campaigns will consume.

Output: A fully functional `3_Pacientes.py` page with all interaction flows from the UI-SPEC.
</objective>

<execution_context>
@/home/rolando/Desarrollo/social-media-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/rolando/Desarrollo/social-media-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-crm-core/03-CONTEXT.md
@.planning/phases/03-crm-core/03-UI-SPEC.md
@.planning/phases/03-crm-core/03-RESEARCH.md
@.planning/phases/03-crm-core/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 components/database.py -->
def get_connection() -> psycopg2.connection
def fetch_patients(search: str = "", tag_ids: list = None, limit: int = 25, offset: int = 0) -> tuple[list[dict], int]
def fetch_existing_phones() -> set[str]
def insert_patients(patients: list[dict]) -> int
def fetch_tags_with_counts() -> list[dict]  # returns [{id, name, color, created_at, patient_count}]
def create_tag(name: str, color: str = "#6366f1") -> dict
def delete_tag(tag_id: str) -> None  # raises ValueError if assigned
def assign_tags_to_patients(patient_ids: list[str], tag_ids: list[str]) -> int

<!-- From Plan 01 components/patients.py -->
def normalize_mx_phone(raw: str) -> tuple[str, str | None]
def parse_import_file(uploaded_file) -> pd.DataFrame  # columns: nombre, apellido, telefono
def build_preview(df: pd.DataFrame, existing_phones: set[str]) -> pd.DataFrame  # adds tel_normalizado, estado

<!-- From components/sidebar.py -->
def render_sidebar() -> None  # must be called at top of every page
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pacientes page — patient list with search, filter, pagination, and tag management</name>
  <files>admin-ui/src/pages/3_Pacientes.py</files>
  <read_first>
    admin-ui/src/pages/2_WhatsApp.py,
    admin-ui/src/components/sidebar.py,
    admin-ui/src/components/database.py,
    admin-ui/src/components/patients.py,
    .planning/phases/03-crm-core/03-UI-SPEC.md,
    .planning/phases/03-crm-core/03-CONTEXT.md
  </read_first>
  <action>
    Create `admin-ui/src/pages/3_Pacientes.py` implementing the full Pacientes page per UI-SPEC and CONTEXT decisions D-01 through D-08.

    **Page structure (top to bottom):**

    1. Imports and sidebar:
       ```python
       import streamlit as st
       import pandas as pd
       from components.sidebar import render_sidebar
       from components.database import (
           fetch_patients, insert_patients, fetch_existing_phones,
           fetch_tags_with_counts, create_tag, delete_tag, assign_tags_to_patients
       )
       from components.patients import parse_import_file, build_preview

       render_sidebar()
       ```

    2. Session state initialization:
       - `st.session_state.setdefault("pacientes_mode", "list")` — values: "list" or "import"
       - `st.session_state.setdefault("current_page", 0)`
       - `st.session_state.setdefault("import_preview", None)` — holds DataFrame during import
       - `st.session_state.setdefault("import_file_processed", False)`

    3. Page header row (per D-03 and UI-SPEC):
       ```python
       col_title, col_btn = st.columns([3, 1])
       with col_title:
           st.title("Pacientes")
       with col_btn:
           if st.session_state.pacientes_mode == "list":
               if st.button("Importar pacientes", icon=":material/upload_file:", use_container_width=True):
                   st.session_state.pacientes_mode = "import"
                   st.session_state.import_preview = None
                   st.session_state.import_file_processed = False
                   st.rerun()
       ```

    4. **Import mode** (when `pacientes_mode == "import"`), per D-01, D-02:
       - Show `st.file_uploader("Sube un archivo CSV o Excel con los datos de pacientes", type=["csv", "xlsx", "xls"], key="import_file")`
       - When file uploaded and not yet processed:
         - Wrap in try/except: call `parse_import_file(uploaded_file)`, then `fetch_existing_phones()`, then `build_preview(df, existing_phones)`
         - On ValueError: show `st.error(str(e))`
         - Store preview DataFrame in `st.session_state.import_preview`
         - Set `st.session_state.import_file_processed = True`
       - When `import_preview` is not None:
         - `st.subheader("Vista previa de importacion")`
         - Display preview DataFrame with `st.dataframe(preview_df, use_container_width=True, hide_index=True)`
         - Compute counts: nuevos = len(preview[preview.estado == "Nuevo"]), duplicados = len(preview[preview.estado == "Duplicado"]), errores = len(preview[preview.estado == "Error"])
         - Show summary: `st.markdown(f"**{nuevos}** nuevos / **{duplicados}** duplicados ignorados / **{errores}** con errores")`
         - Two buttons in columns:
           - "Importar {nuevos} nuevos" button (disabled if nuevos == 0)
           - "Descartar importacion" button
         - On "Importar" click:
           - Filter preview to estado=="Nuevo" rows
           - Build patients list of dicts: `{"first_name": row.nombre, "last_name": row.apellido, "phone": str(row.telefono), "phone_normalized": row.tel_normalizado}`
           - Call `insert_patients(patients)` inside `st.spinner("Importando pacientes...")`
           - Show `st.success(f"Se importaron {count} pacientes exitosamente.")`
           - Reset mode to "list", clear import_preview, rerun
         - On "Descartar" click: reset mode to "list", clear import_preview, rerun

    5. **List mode** (when `pacientes_mode == "list"`):

       a. **Filter bar** (per D-04, D-07):
          - `st.columns([3, 2, 1])`:
            - Col 1: `search = st.text_input("", placeholder="Buscar por nombre o telefono...", key="patient_search")`
            - Col 2: Fetch tags via `fetch_tags_with_counts()`, create `tag_options = {t["name"]: str(t["id"]) for t in tags}`, show `st.multiselect("Filtrar por etiquetas", options=list(tag_options.keys()), key="tag_filter")`
            - Col 3: nothing (or use for spacing)

       b. **Tag management expander** (per D-07, D-08):
          - `with st.expander("Nueva etiqueta"):`
            - `tag_name = st.text_input("Nombre de etiqueta", max_chars=50, key="new_tag_name")`
            - `tag_color = st.color_picker("Color", value="#6366f1", key="new_tag_color")`
            - `if st.button("Crear etiqueta", disabled=not tag_name.strip()): create_tag(tag_name.strip(), tag_color); st.rerun()`
          - Below expander, show existing tags as inline items:
            - For each tag from `fetch_tags_with_counts()`:
              - Display tag name with color indicator, patient count
              - If patient_count == 0: show delete button
              - If patient_count > 0 and delete attempted: show `st.warning(f"Esta etiqueta esta asignada a {t['patient_count']} pacientes. Elimina las asignaciones primero.")`

       c. **Patient data table** (per D-04, D-05, D-06):
          - Resolve selected tag IDs from multiselect names
          - Call `fetch_patients(search=search, tag_ids=selected_tag_ids, limit=25, offset=st.session_state.current_page * 25)`
          - If no patients and no filters: show empty state per UI-SPEC ("No hay pacientes registrados" + "Importa tu primer archivo..." + import CTA button)
          - If no patients with filters: show `st.info("No se encontraron pacientes con ese criterio de busqueda.")`
          - If patients found:
            - Build display DataFrame with columns: id (hidden later), Nombre (first_name + " " + last_name), Telefono (phone_normalized), Etiquetas (fetch patient tags joined), Fuente (source), Registrado (created_at formatted as "01 abr 2026")
            - For Etiquetas column: run a separate query or join patient_tags+tags for the current page's patient IDs to get tag names per patient, comma-separated
            - Display with `st.dataframe(display_df[visible_columns], use_container_width=True, hide_index=True, on_select="rerun", selection_mode="multi-row", key="patient_table")`

       d. **Bulk action bar** (per D-06, visible when rows selected):
          - Check `st.session_state.get("patient_table")` for selection
          - If rows selected:
            - Map selected indices to patient UUIDs: `selected_ids = [display_df.iloc[i]["id"] for i in event.selection.rows]`
            - `st.caption(f"{len(selected_ids)} pacientes seleccionados")`
            - `tag_assign = st.multiselect("Etiquetas a asignar", options=[t["name"] for t in tags], key="assign_tags")`
            - `if st.button("Asignar etiquetas", disabled=not tag_assign):`
              - Resolve tag names to IDs
              - Call `assign_tags_to_patients(selected_ids, selected_tag_ids)`
              - `st.success(f"Etiquetas asignadas a {len(selected_ids)} pacientes.")`
              - `st.rerun()`

       e. **Pagination** (per D-05):
          - PAGE_SIZE = 25
          - `total_pages = max(1, (total + PAGE_SIZE - 1) // PAGE_SIZE)`
          - Three columns: Anterior button (disabled if page==0), page caption "Mostrando {start}-{end} de {total} pacientes", Siguiente button (disabled if last page)
          - Buttons update `st.session_state.current_page` and call `st.rerun()`
          - Reset current_page to 0 when search or tag filter changes

    **Important implementation notes:**
    - Fetch tags for patient display: after fetching patients page, get tag names per patient with a single query: `SELECT pt.patient_id, t.name, t.color FROM patient_tags pt JOIN tags t ON pt.tag_id = t.id WHERE pt.patient_id = ANY(%s)` using the page's patient IDs. Group by patient_id in Python.
    - Use `try/except` around all DB calls with `st.error()` fallback.
    - The `st.dataframe` `event` object is accessed via the return value of `st.dataframe(..., key="patient_table")`.
    - When search or tag filter changes, reset `current_page` to 0 to avoid showing empty pages.
  </action>
  <verify>
    <automated>cd /home/rolando/Desarrollo/social-media-manager/admin-ui/src && python -c "import pages.3_Pacientes" 2>&1 || echo "Import check requires Streamlit context, verifying syntax instead" && python -m py_compile pages/3_Pacientes.py</automated>
  </verify>
  <acceptance_criteria>
    - admin-ui/src/pages/3_Pacientes.py exists with 200+ lines
    - File contains `from components.sidebar import render_sidebar`
    - File contains `render_sidebar()`
    - File contains `from components.database import`
    - File contains `from components.patients import`
    - File contains `st.title("Pacientes")`
    - File contains `st.file_uploader` with type=["csv", "xlsx", "xls"]
    - File contains `"Importar pacientes"` button text
    - File contains `"Vista previa de importacion"`
    - File contains `st.dataframe` with `on_select="rerun"` and `selection_mode="multi-row"`
    - File contains `"Buscar por nombre o telefono"` (search placeholder)
    - File contains `"Filtrar por etiquetas"` (multiselect label)
    - File contains `"Asignar etiquetas"` button text
    - File contains `"Nueva etiqueta"` expander
    - File contains `st.color_picker`
    - File contains `"Crear etiqueta"` button text
    - File contains `"Esta etiqueta esta asignada a"` (D-08 blocking message)
    - File contains `"No hay pacientes registrados"` (empty state)
    - File contains `"Anterior"` and `"Siguiente"` (pagination buttons)
    - File contains `"Mostrando"` (page caption pattern)
    - File contains `PAGE_SIZE = 25` or `limit=25`
    - `python -m py_compile pages/3_Pacientes.py` exits 0
  </acceptance_criteria>
  <done>The Pacientes page renders with all interaction flows: CSV/Excel import with preview and confirmation (D-01, D-02, D-03), paginated patient list with search and tag filter (D-04, D-05), bulk tag assignment via row selection (D-06), inline tag creation with color picker (D-07), and tag deletion blocking (D-08). All copy matches the UI-SPEC copywriting contract.</done>
</task>

</tasks>

<verification>
1. Syntax check: `cd /home/rolando/Desarrollo/social-media-manager/admin-ui/src && python -m py_compile pages/3_Pacientes.py`
2. All existing tests still pass: `python -m pytest tests/ -x -q`
3. File imports resolve correctly (no circular imports)
</verification>

<success_criteria>
- 3_Pacientes.py is a complete, syntactically valid Streamlit page
- All 12 decisions (D-01 through D-08 relevant to this page) are implemented
- Page follows established pattern: render_sidebar() at top, session_state for mode management
- All DB operations use the database.py helper (no raw psycopg2 in the page)
- All business logic uses patients.py helper (no normalization logic in the page)
- Copy matches UI-SPEC copywriting contract exactly
</success_criteria>

<output>
After completion, create `.planning/phases/03-crm-core/03-02-SUMMARY.md`
</output>
